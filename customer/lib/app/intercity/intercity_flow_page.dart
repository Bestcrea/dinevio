import 'package:flutter/material.dart';

class IntercityFlowPage extends StatelessWidget {
  const IntercityFlowPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Intercity coming soon')),
    );
  }
}
